-- ============================================================
-- Diagnostic : Dump user credentials (id, email, hashed_password)
-- ============================================================

SELECT id, email, hashed_password FROM users;
